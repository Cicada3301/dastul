define(function () {
    return true;
})
/*define(['entities/entity'], function(Entity){
    function Player(world){
        //Entity.call(this, 1, 2, world.pos.x+Math.floor(world.pos.x/2), world.pos.y+Math.floor(world.pos.y/2));
    }
    return Player;
});*/